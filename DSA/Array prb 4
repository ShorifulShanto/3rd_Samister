Check if a specific element exists in the array and, if so, report its index.

#include <stdio.h>

int main() {
    int arr[] = {10, 20, 30, 40, 50};
    int i, n = 5;
    int search = 30;
    
    int a=-1;

    for(i = 0; i < n; i++) {
        if(arr[i] == search) {
            printf(" found at index %d\n", i);
            
            break;
        }
        
    } 
        if(arr[i]=! search) {
        printf(" not found in the array%d\n",a);
        } 

    return 0;
}
